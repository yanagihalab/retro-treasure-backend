const qs = (s) => document.querySelector(s);
const state = { token: localStorage.getItem('retro_token') || '', history: [] };
function sleep(ms){return new Promise((resolve)=>setTimeout(resolve,ms));}
function setText(selector, value){ const el = qs(selector); if (el) el.textContent = value; }
function initials(name){ if(!name) return '??'; const parts=String(name).trim().split(/\s+/); if(parts.length>=2) return (parts[0][0]+parts[1][0]).toUpperCase(); return String(name).slice(0,2).toUpperCase(); }
function updateClock(){ const now=new Date(); setText('#clockText', now.toLocaleTimeString('ja-JP',{hour:'2-digit',minute:'2-digit'})); }
async function api(path, options={}){ const headers={'Content-Type':'application/json','Authorization':`Bearer ${state.token}`,...(options.headers||{})}; const response=await fetch(path,{...options,headers}); const ct=response.headers.get('content-type')||''; const data=ct.includes('application/json')?await response.json():await response.text(); if(!response.ok) throw new Error(data?.error || `request failed: ${response.status}`); return data; }
async function loadPlayerInfo(){ const data=await api('/api/player/me'); setText('#gachaCoinsText', `COIN: ${data.coins ?? 0}`); }
function renderHistory(){ const root=qs('#gachaHistory'); if(!root) return; if(!state.history.length){ root.innerHTML='<div class="list-item">結果はここに表示されます。</div>'; return; } root.innerHTML=state.history.map((item)=>`<div class="list-item"><strong>${item.name}</strong><div class="meta">属性: ${item.element} / レア: ${'★'.repeat(Math.max(1, item.rarity || 1))}</div></div>`).join(''); }
async function drawGacha(){ const box=qs('#gachaResultBox'); const btn=qs('#drawGachaBtn'); if(btn) btn.disabled=true; try{ if(box) box.textContent='通信中...'; await sleep(250); const result=await api('/api/gacha/draw',{method:'POST',body:JSON.stringify({})}); const card=result.card || result.reward_card || result.result_card || null; if(card){ setText('#gachaResultInitials', initials(card.name)); if(box){ box.innerHTML=`<h3>${card.name}</h3><p>属性: ${card.element}</p><p>レア: ${'★'.repeat(Math.max(1, card.rarity || 1))}</p><p>HP ${card.max_hp} / ATK ${card.attack} / DEF ${card.defense}</p>`; } state.history.unshift(card); state.history=state.history.slice(0,10); renderHistory(); } else { if(box) box.textContent='カード取得結果を表示できませんでした。'; } await loadPlayerInfo(); } catch(err){ if(box) box.textContent=`ERROR: ${err.message}`; } finally { if(btn) btn.disabled=false; } }
function bindEvents(){ const drawBtn=qs('#drawGachaBtn'); if(drawBtn) drawBtn.addEventListener('click', drawGacha); const reloadBtn=qs('#reloadGachaBtn'); if(reloadBtn) reloadBtn.addEventListener('click', async ()=>{ try{ await loadPlayerInfo(); } catch(err){ const box=qs('#gachaResultBox'); if(box) box.textContent=`ERROR: ${err.message}`; } }); }
async function init(){ updateClock(); setInterval(updateClock,30000); bindEvents(); if(!state.token){ const box=qs('#gachaResultBox'); if(box) box.textContent='先にトップ画面でログインしてください。'; return; } try{ await loadPlayerInfo(); renderHistory(); } catch(err){ const box=qs('#gachaResultBox'); if(box) box.textContent=`ERROR: ${err.message}`; } }
init();
